export enum PROPERTIES {
    BOOLEAN = 'Boolean',
    INTEGER = 'Integer',
    ENUM = 'Enum',
    RANGE = 'Range',
    COLOR = 'Color',
}

export enum PROPERTIES_LOWERCASE {
    BOOLEAN = 'boolean',
    INTEGER = 'integer',
    ENUM = 'enum',
    RANGE = 'range',
    COLOR = 'color',
}